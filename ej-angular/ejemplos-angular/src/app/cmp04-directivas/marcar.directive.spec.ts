import { MarcarDirective } from './marcar.directive';

describe('MarcarDirective', () => {
  it('should create an instance', () => {
    const directive = new MarcarDirective();
    expect(directive).toBeTruthy();
  });
});
