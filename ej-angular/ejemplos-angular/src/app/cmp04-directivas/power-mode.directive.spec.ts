import { PowerModeDirective } from './power-mode.directive';

describe('PowerModeDirective', () => {
  it('should create an instance', () => {
    const directive = new PowerModeDirective();
    expect(directive).toBeTruthy();
  });
});
